[pytest]
testpaths = tests
pythonpath = .

addopts =
    -ra
    --tb=short
    --strict-markers

markers =
    smoke: critical smoke tests
    regression: full regression suite
    negative: negative/error handling tests